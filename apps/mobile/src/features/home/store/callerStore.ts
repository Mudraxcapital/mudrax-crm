/**
 * @deprecated Prefer `useLeadWorkflowStore` from `@/features/leads/store/leadWorkflowStore`.
 * Kept so transitional imports continue to resolve.
 */
export { useLeadWorkflowStore as useCallerStore } from "@/features/leads/store/leadWorkflowStore";
