# Deploy

Everything needed to run Mudrax CRM on the company's Linux server.

- `pm2/` - PM2 ecosystem configuration.
- `nginx/` - Nginx reverse-proxy configuration templates.
- `env/` - sample environment files per environment.

**Never put here**: real secrets. Only templates/examples are committed to git.
