# deploy/env

Sample environment files per deployment environment (`.env.production.example`, `.env.staging.example`).

Copy the relevant template to the actual server location and fill in real values. Never commit a populated `.env` file.
