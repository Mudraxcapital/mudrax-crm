# e2e/
#
# Playwright end-to-end suite for production-hardening workflows.
# Run: npm run test:e2e
# Requires seeded DB (npm run db:seed) and either a running app
# (E2E_SKIP_WEBSERVER=1) or a prior `npm run build` so webServer can
# start `next start`.
