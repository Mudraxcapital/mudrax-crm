# integrations/telephony

PBX/VoIP/GSM provider adapters. Every adapter here implements the `telephony` module's `ITelephonyProvider` port - this is the seam that allows the provider to be replaced without touching business logic.
