# domain/repositories

Repository **interfaces** (ports) for the `loan-accounts` module — the contract this module needs for persistence, with no implementation.

**Never put here**: Prisma Client usage or any concrete implementation — that belongs in `infrastructure/repositories`.
