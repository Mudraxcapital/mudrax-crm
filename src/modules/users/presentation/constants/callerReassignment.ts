/** Sentinel value for UI + API — Callers become Direct Admin (no Team Lead). */
export const REASSIGN_CALLERS_TO_DIRECT_ADMIN = "direct-admin" as const;

export const DIRECT_ADMIN_REASSIGN_LABEL = "Direct Admin (no Team Lead)";
