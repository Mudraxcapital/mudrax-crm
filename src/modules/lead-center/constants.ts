// ============================================================================
// src/modules/lead-center/constants.ts
//
// Client-safe constants (no Prisma / Node deps). UI may import from here.
// ============================================================================

/** Matches bulk staged-lead action limits. */
export const BULK_MAX = 200;
