# domain/errors

Domain-specific error types for the `banks` module (e.g. business-rule violations).

**Never put here**: generic/framework errors (HTTP errors, Prisma errors) — translate those at the boundary instead.
