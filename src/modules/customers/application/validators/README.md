# application/validators

Zod schemas validating use-case input for the `customers` module.

**Never put here**: UI form logic (React Hook Form wiring lives in `presentation/`) — these schemas may be reused there, but they are owned here.
