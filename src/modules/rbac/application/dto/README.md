# application/dto

Data Transfer Objects for the `rbac` module's use-cases — the shape of input/output crossing the application boundary.

**Never put here**: Prisma models or domain entities directly — DTOs are a deliberate, separate shape.
