# infrastructure/repositories

Prisma-backed implementations of the `rbac` module's `domain/repositories` interfaces.

**Never put here**: business rules/decisions — only persistence and translation.
