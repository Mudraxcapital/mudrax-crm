# domain/entities

Domain entities for the `documents` module.

Plain TypeScript classes/types describing the core business objects of this module and their invariants. Zero framework dependencies.

**Never put here**: Prisma models/types, React, Next.js imports, `fetch`, or any persistence/HTTP concern.
