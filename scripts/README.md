# Scripts

Operational scripts (seed runner, backup trigger, one-off data-fix scripts).

**Never put here**: business logic the app itself needs at runtime - if the app needs it, it belongs in a module.
