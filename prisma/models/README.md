# prisma/models

One `.prisma` file per module will live here, mirroring `src/modules/*` (e.g. `leads.prisma`, `loan-applications.prisma`).

No models exist yet - this folder is intentionally empty pending schema design approval.
