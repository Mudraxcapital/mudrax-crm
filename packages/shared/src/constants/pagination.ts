/** Default page size used by list API clients (matches web Route Handlers). */
export const DEFAULT_PAGE_SIZE = 100;

/** Hard upper bound for list API page size. */
export const MAX_PAGE_SIZE = 500;

export const DEFAULT_PAGE_OFFSET = 0;
